import {StyleSheet} from 'react-native';

const RowContainerStyle = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});

export default RowContainerStyle;
